# FootyStats local cache

This directory is intentionally empty in Git.

Run the guarded import once from PowerShell. Raw responses and the normalized
Premier League match history are stored here and ignored by Git. Dashboard
recalculation reads only these cached files and never calls FootyStats.
